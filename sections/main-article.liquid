{{ 'section-blog-post.css' | asset_url | stylesheet_tag }}
{{- 'component-rte.css' | asset_url | stylesheet_tag -}}
<script src="{{ 'copy.js' | asset_url }}" defer="defer"></script>

{%- style -%}
  {% render 'section-margin' %}
  {% render 'section-padding' %}
{%- endstyle -%}

<article
  class="article-template section-{{ section.id }}-padding{% if section.settings.show_border_top %} article-template--border-top{% endif %}"
  itemscope
  itemtype="http://schema.org/BlogPosting"
>
  <div class="container">
    <div class="article-template__container {% if section.settings.margin_top == 'no-indent' %} article-template__container_margin  {% endif %}">
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when '@app' -%}
            <div
              class="page-width--narrow"
              {{ block.shopify_attributes }}
            >
              {% render block %}
            </div>
          {%- when 'article-header' -%}
            <header
              class="article-template__block article-template__header"
              {{ block.shopify_attributes }}
            >
              <div class="article-template__header-wrapper">
                {%- if block.settings.blog_show_tags
                  or block.settings.blog_show_date
                  or block.settings.blog_show_author
                -%}
                  <div class="article-template__header-meta">
                    {% comment %} {%- if block.settings.blog_show_tags
                      and article.tags != blank
                    -%}
                      <div class="article-template__tags">
                        {% for tag in article.tags %}
                          <a href="{{ blog.url }}/tagged/{{ tag | handle }}" class="article-template__tag button button--primary">
                            <span title="{{ tag }}">
                              {{- tag }}
                            </span>
                          </a>
                        {% endfor %}
                      </div>
                    {%- endif -%} {% endcomment %}
                    {%- if block.settings.blog_show_date
                      or block.settings.blog_show_author
                    -%}
                      <div class="article-template__header-info">
                        {%- if block.settings.blog_show_date -%}
                          <span class="article-template__date subheading">
                            {{- article.published_at | date: format: 'abbreviated_date' -}}
                          </span>
                        {%- endif -%}
                        {%- if block.settings.blog_show_author -%}
                          {%- if block.settings.blog_show_date == true -%}
                            {% render 'icon-bullet' %}
                          {%- endif -%}
                          <div class="article-template__author subheading">
                            <span>
                              {{- article.author -}}
                            </span>
                          </div>
                        {%- endif -%}
                      </div>
                    {%- endif -%}
                  </div>
                {%- endif -%}
                {% comment %} {% assign section_heading_tag = block.settings.section_heading_tag %}
                <{{section_heading_tag}} class="article-template__header-title {{ block.settings.heading_size }}"
                  id="Article-{{ article.id }}"
                >
                  {{ article.title | escape }}
                </{{section_heading_tag}}> {% endcomment %}
                {%- if block.settings.blog_show_excerpt
                  and article.excerpt.size > 0
                -%}
                  <div class="article-template__excerpt rte{% if settings.rte_type == 'custom' %} rte__container{% endif %}">
                    {{ article.excerpt }}
                  </div>
                {%- endif -%}
                {% comment %} COLORED TAGS {% endcomment %}
                {% assign metaobject_colored_tags = metaobjects.article_colored_tags.values %}
                <div class="colored_tags_wrapper">
                  {% for tag in article.tags %}
                    {% for tag_item in metaobject_colored_tags %}
                      {% assign tag_item_color = tag_item.tag_color %}
                      {% assign tag_item_name = tag_item.tag_name %}

                      {% if tag_item_name == tag %}
                        <style>
                          .colored_tag.{{ tag_item_name | handleize }} {
                            background-color: {{ tag_item_color.value | hex_to_rgba: 0.1 }};
                            border-color: {{ tag_item_color }};
                            color: {{ tag_item_color }};
                          }
                        </style>
                        <div class="colored_tag {{ tag_item_name | handleize }}">
                          {{ tag }}
                        </div>
                      {% endif %}
                    {% endfor %}
                  {% endfor %}
                </div>
                {% comment %} COLORED TAGS {% endcomment %}
                {% if block.settings.show_share %}
                  {%- if block.settings.show_share_facebook
                    or block.settings.show_share_twitter
                    or block.settings.show_share_telegram
                    or block.settings.show_share_reddit
                    or block.settings.show_share_copy
                  -%}
                    <div class="article-template__header-share">
                      <ul class="share-buttons-simple">
                        {%- if block.settings.show_share_facebook -%}
                          <li class="share-buttons-simple__item">
                            <a
                              href="https://www.facebook.com/sharer.php?u={{ shop.url | append: article.url }}"
                              class="share-buttons-simple__button facebook-btn focus-inset"
                              target="_blank"
                            >
                              {% render 'icon-facebook' %}
                              {% if block.settings.show_share_label %}
                                <span>
                                  {{ 'general.social.links.facebook' | t }}
                                </span>
                              {% endif %}
                            </a>
                          </li>
                        {%- endif -%}

                        {%- if block.settings.show_share_twitter -%}
                          <li class="share-buttons-simple__item">
                            <a
                              href="https://twitter.com/share?url={{ shop.url | append: article.url }}&text={{ article.title | escape }}"
                              class="share-buttons-simple__button twitter-btn focus-inset"
                              target="_blank"
                            >
                              {% render 'icon-twitter' %}
                              {% if block.settings.show_share_label %}
                                <span>
                                  {{ 'general.social.links.twitter' | t }}
                                </span>
                              {% endif %}
                            </a>
                          </li>
                        {%- endif -%}

                        {%- if block.settings.show_share_telegram -%}
                          <li class="share-buttons-simple__item">
                            <a
                              href="https://t.me/share/url?url={{ shop.url | append: article.url }}&text={{ article.title | escape }}"
                              class="share-buttons-simple__button telegram-btn focus-inset"
                              target="_blank"
                            >
                              {% render 'icon-telegram' %}
                              {% if block.settings.show_share_label %}
                                <span>
                                  {{ 'general.social.links.telegram' | t }}
                                </span>
                              {% endif %}
                            </a>
                          </li>
                        {%- endif -%}

                        {%- if block.settings.show_share_reddit -%}
                          <li class="share-buttons-simple__item">
                            <a
                              href="https://reddit.com/submit?url={{ shop.url | append: article.url }}&text={{ article.title | escape }}"
                              class="share-buttons-simple__button pinterest-btn focus-inset"
                              target="_blank"
                            >
                              {% render 'icon-reddit' %}
                              {% if block.settings.show_share_label %}
                                <span>
                                  {{ 'general.social.links.reddit' | t }}
                                </span>
                              {% endif %}
                            </a>
                          </li>
                        {%- endif -%}

                        {%- if block.settings.show_share_copy -%}
                          <li class="share-buttons-simple__item">
                            <a
                              href="{{ shop.url | append: article.url }}"
                              class="share-buttons-simple__button copy-btn focus-inset"
                              data-notification="{{ 'general.notification.copied_link' | t }}"
                            >
                              {% render 'icon-share' %}
                              {% if block.settings.show_share_label %}
                                <span>
                                  {{ 'general.social.links.copy_link' | t }}
                                </span>
                              {% endif %}
                            </a>
                          </li>
                        {%- endif -%}
                      </ul>
                    </div>
                  {%- endif -%}
                {% endif %}
              </div>
              {% if block.settings.blog_show_image and article.image != blank %}
                {%- liquid
                  if block.settings.image_ratio == 'adapt'
                    assign aspect_ratio = article.image.aspect_ratio
                  else
                    assign aspect_ratio = block.settings.image_ratio
                  endif
                -%}
                <div class="article-template__header-image-wrapper">
                  <div
                    class="article-template__header-image"
                    style="--aspect-ratio: {{aspect_ratio}};"
                  >
                    {{
                      article.image
                      | image_url: width: article.image.width
                      | image_tag:
                        loading: 'lazy',
                        sizes: '100vw',
                        widths: '375, 550, 750, 1100, 1500, 1780, 2000, 3000, 3840'
                    }}
                  </div>
                </div>
              {% endif %}
              {% comment %} title {% endcomment %}
               {% assign section_heading_tag = block.settings.section_heading_tag %}
              <{{section_heading_tag}} class="article-template__header-title {{ block.settings.heading_size }}"
                id="Article-{{ article.id }}"
              >
                {{ article.title | escape }}
              </{{section_heading_tag}}>
            </header>
          {%- when 'content' -%}
            <div
              class="article-template__block article-template__content"
              itemprop="articleBody"
              {{ block.shopify_attributes }}
            >
              <div class="article-template__content-wrapper">
                <div class="article-template__content-rte rte{% if settings.rte_type == 'custom' %} rte__container{% endif %}">
                  {{ article.content }}
                </div>
              </div>
            </div>
          {%- when 'article-footer' -%}
            <div
              class="article-template__block article-template__footer"
              {{ block.shopify_attributes }}
            >
              <div class="article-template__footer-wrapper">
                {%- if block.settings.blog_show_date-%}
                  <div class="article-template__date">
                    {{- 'general.blog.update' | t }}
                    {{- article.updated_at | date: '%B %d, %Y' -}}
                  </div>
                {% endif %}
                {% if block.settings.show_share %}
                  {%- if block.settings.show_share_facebook
                    or block.settings.show_share_twitter
                    or block.settings.show_share_telegram
                    or block.settings.show_share_reddit
                    or block.settings.show_share_copy
                  -%}
                    <div class="article-template__share">
                      <ul class="share-buttons-simple">
                        {%- if block.settings.show_share_facebook -%}
                          <li class="share-buttons-simple__item">
                            <a
                              href="https://www.facebook.com/sharer.php?u={{ shop.url | append: article.url }}"
                              class="share-buttons-simple__button facebook-btn focus-inset"
                              target="_blank"
                            >
                              {% render 'icon-facebook' %}
                              {% if block.settings.show_share_label %}
                                <span>
                                  {{ 'general.social.links.facebook' | t }}
                                </span>
                              {% endif %}
                            </a>
                          </li>
                        {%- endif -%}

                        {%- if block.settings.show_share_twitter -%}
                          <li class="share-buttons-simple__item">
                            <a
                              href="https://twitter.com/share?url={{ shop.url | append: article.url }}&text={{ article.title | escape }}"
                              class="share-buttons-simple__button twitter-btn focus-inset"
                              target="_blank"
                            >
                              {% render 'icon-twitter' %}
                              {% if block.settings.show_share_label %}
                                <span>
                                  {{ 'general.social.links.twitter' | t }}
                                </span>
                              {% endif %}
                            </a>
                          </li>
                        {%- endif -%}

                        {%- if block.settings.show_share_telegram -%}
                          <li class="share-buttons-simple__item">
                            <a
                              href="https://t.me/share/url?url={{ shop.url | append: article.url }}&text={{ article.title | escape }}"
                              class="share-buttons-simple__button telegram-btn focus-inset"
                              target="_blank"
                            >
                              {% render 'icon-telegram' %}
                              {% if block.settings.show_share_label %}
                                <span>
                                  {{ 'general.social.links.telegram' | t }}
                                </span>
                              {% endif %}
                            </a>
                          </li>
                        {%- endif -%}

                        {%- if block.settings.show_share_reddit -%}
                          <li class="share-buttons-simple__item">
                            <a
                              href="https://reddit.com/submit?url={{ shop.url | append: article.url }}&text={{ article.title | escape }}"
                              class="share-buttons-simple__button pinterest-btn focus-inset"
                              target="_blank"
                            >
                              {% render 'icon-reddit' %}
                              {% if block.settings.show_share_label %}
                                <span>
                                  {{ 'general.social.links.reddit' | t }}
                                </span>
                              {% endif %}
                            </a>
                          </li>
                        {%- endif -%}

                        {%- if block.settings.show_share_copy -%}
                          <li class="share-buttons-simple__item">
                            <a
                              href="{{ shop.url | append: article.url }}"
                              class="share-buttons-simple__button copy-btn focus-inset"
                              data-notification="{{ 'general.notification.copied_link' | t }}"
                            >
                              {% render 'icon-share' %}
                              {% if block.settings.show_share_label %}
                                <span>
                                  {{ 'general.social.links.copy_link' | t }}
                                </span>
                              {% endif %}
                            </a>
                          </li>
                        {%- endif -%}
                      </ul>
                    </div>
                  {%- endif -%}
                {% endif %}
              </div>
            </div>
        {%- endcase -%}
      {%- endfor -%}
    </div>
  </div>

  {%- if blog.comments_enabled? -%}
    <div class="container">
      <div class="article-template__comment-wrapper">
        <div id="comments" class="article-template__comment-container">
          {%- if article.comments_count > 0 -%}
            {%- assign anchorId = '#Comments-' | append: article.id -%}

            <h3 id="Comments-{{ article.id }}" tabindex="-1">
              {{ 'blogs.article.comments' | t: count: article.comments_count }}
            </h3>
            {% paginate article.comments by 5 %}
              <div class="article-template__comments">
                {%- if comment.status == 'pending' and comment.content -%}
                  <article
                    id="{{ comment.id }}"
                    class="article-template__comments-comment"
                  >
                    {{ comment.content }}
                    <footer class="right">
                      <span class="circle-divider caption-with-letter-spacing">
                        {{- comment.author -}}
                      </span>
                    </footer>
                  </article>
                {%- endif -%}

                {%- for comment in article.comments -%}
                  <article
                    id="{{ comment.id }}"
                    class="article-template__comments-comment"
                  >
                    {{ comment.content }}
                    <footer class="right">
                      <span class="circle-divider caption-with-letter-spacing">
                        {{- comment.author -}}
                      </span>
                      <span class="caption-with-letter-spacing">
                        {{- comment.created_at | time_tag: format: 'date' -}}
                      </span>
                    </footer>
                  </article>
                {%- endfor -%}
                {% render 'pagination', paginate: paginate, anchor: anchorId %}
              </div>
            {% endpaginate %}
          {%- endif -%}
          {% form 'new_comment', article %}
            {%- liquid
              assign post_message = 'blogs.article.success'
              if blog.moderated? and comment.status == 'unapproved'
                assign post_message = 'blogs.article.success_moderated'
              endif
            -%}
            <h3>{{ 'blogs.article.comment_form_title' | t }}</h3>
            {%- if form.errors -%}
              <div class="form__message" role="alert">
                <h4
                  class="form-status caption-large text-body"
                  tabindex="-1"
                  autofocus
                >
                  {%- render 'icon-error' -%}
                  {{ 'templates.contact.form.error_heading' | t }}
                </h4>
              </div>
              <ul class="form-status-list caption-large">
                {%- for field in form.errors -%}
                  <li>
                    <a href="#CommentForm-{{ field }}" class="link">
                      {%- if form.errors.translated_fields[field]
                          contains 'author'
                      -%}
                        {{ 'blogs.article.name' | t }}
                      {%- elsif form.errors.translated_fields[field]
                          contains 'body'
                      -%}
                        {{ 'blogs.article.message' | t }}
                      {%- else -%}
                        {{ form.errors.translated_fields[field] }}
                      {%- endif -%}
                      {{ form.errors.messages[field] }}
                    </a>
                  </li>
                {%- endfor -%}
              </ul>
            {%- elsif form.posted_successfully? -%}
              <div class="form-status-list form__message" role="status">
                <h4 class="form-status" tabindex="-1" autofocus>
                  {% render 'icon-success' %}
                  {{ post_message | t }}
                </h4>
              </div>
            {%- endif -%}

            <div
              {% if blog.moderated? == false %}
                class="article-template__comments-fields"
              {% endif %}
            >
              <div class="article-template__comment-fields">
                <div class="field field--with-error">
                  <input
                    dir="ltr"
                    type="text"
                    name="comment[author]"
                    id="CommentForm-author"
                    class="field__input"
                    autocomplete="name"
                    value="{{ form.author }}"
                    aria-required="true"
                    required
                    {% if form.errors contains 'author' %}
                      aria-invalid="true"
                      aria-describedby="CommentForm-author-error"
                    {% endif %}
                    placeholder="{{ 'blogs.article.name' | t }}"
                  >
                  <label for="CommentForm-author">
                    {{- 'blogs.article.name' | t -}}
                  </label>
                  {%- if form.errors contains 'author' -%}
                    <small id="CommentForm-author-error">
                      <span class="form__message">
                        {%- render 'icon-error' -%}
                        {{- 'blogs.article.name' | t }}
                        {{ form.errors.messages.author }}.</span
                      >
                    </small>
                  {%- endif -%}
                </div>
                <div class="field field--with-error">
                  <input
                    dir="ltr"
                    type="email"
                    name="comment[email]"
                    id="CommentForm-email"
                    class="field__input"
                    autocomplete="email"
                    value="{{ form.email }}"
                    autocorrect="off"
                    autocapitalize="off"
                    aria-required="true"
                    required
                    {% if form.errors contains 'email' %}
                      aria-invalid="true"
                      aria-describedby="CommentForm-email-error"
                    {% endif %}
                    placeholder="{{ 'blogs.article.email' | t }}"
                  >
                  <label for="CommentForm-email">
                    {{- 'blogs.article.email' | t -}}
                  </label>
                  {%- if form.errors contains 'email' -%}
                    <small id="CommentForm-email-error">
                      <span class="form__message">
                        {%- render 'icon-error' -%}
                        {{- 'blogs.article.email' | t }}
                        {{ form.errors.messages.email }}.</span
                      >
                    </small>
                  {%- endif -%}
                </div>
              </div>
              <div class="field field--with-error">
                <textarea
                  rows="5"
                  name="comment[body]"
                  id="CommentForm-body"
                  class="text-area field__input"
                  aria-required="true"
                  required
                  {% if form.errors contains 'body' %}
                    aria-invalid="true"
                    aria-describedby="CommentForm-body-error"
                  {% endif %}
                  placeholder="{{ 'blogs.article.message' | t }}"
                >{{ form.body }}</textarea>
                <label for="CommentForm-body">
                  {{- 'blogs.article.message' | t -}}
                </label>
              </div>
              {%- if form.errors contains 'body' -%}
                <small id="CommentForm-body-error">
                  <span class="form__message">
                    {%- render 'icon-error' -%}
                    {{- 'blogs.article.message' | t }}
                    {{ form.errors.messages.body }}.</span
                  >
                </small>
              {%- endif -%}
            </div>
            {%- if blog.moderated? -%}
              <p class="article-template__comment-warning caption">
                {{ 'blogs.article.moderated' | t }}
              </p>
            {%- endif -%}
            <button
              type="submit"
              class="button button--primary button--primary-size focus-inset"
              data-button-text="{{ 'blogs.article.post' | t }}"
            >
              <span>{{ 'blogs.article.post' | t }}</span>
            </button>
          {% endform %}
        </div>
      </div>
    </div>
  {%- endif -%}
</article>

<script type="application/ld+json">
  {
    "@context": "http://schema.org",
    "@type": "Article",
    "articleBody": {{ article.content | strip_html | json }},
    "mainEntityOfPage": {
      "@type": "WebPage",
      "@id": {{ request.origin | append: page.url | json }}
    },
    "headline": {{ article.title | json }},
    {% if article.excerpt != blank %}
      "description": {{ article.excerpt | strip_html | json }},
    {% endif %}
    {% if article.image %}
  			{% assign image_size = article.image.width | append: 'x' %}
      "image": [
        {{ article | image_url: image_size | prepend: "https:" | json }}
      ],
    {% endif %}
    "datePublished": {{ article.published_at | date: '%Y-%m-%dT%H:%M:%SZ' | json }},
    "dateCreated": {{ article.created_at | date: '%Y-%m-%dT%H:%M:%SZ' | json }},
    "author": {
      "@type": "Person",
      "name": {{ article.author | json }}
    },
    "publisher": {
      "@type": "Organization",
      {% if settings.share_image %}
  				{% assign image_size = settings.share_image.width | append: 'x' %}
        "logo": {
          "@type": "ImageObject",
          "height": {{ settings.share_image.height | json }},
          "url": {{ settings.share_image | image_url: image_size | prepend: "https:" | json }},
          "width": {{ settings.share_image.width | json }}
        },
      {% endif %}
      "name": {{ shop.name | json }}
    }
  }
</script>

{% schema %}
{
  "name": "t:sections.main-article.name",
  "tag": "section",
  "class": "section-main-article spaced-section",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.all.border.header.content"
    },
    {
      "type": "checkbox",
      "id": "show_border_top",
      "default": true,
      "label": "t:sections.all.border.border_enable_top.label"
    },
    {
      "type": "header",
      "content": "t:sections.all.section-margin.header.content"
    },
    {
      "type": "select",
      "id": "margin_top",
      "options": [
        {
          "value": "no-indent",
          "label": "t:sections.all.section-margin.options__1.label"
        },
        {
          "value": "xs",
          "label": "t:sections.all.section-margin.options__2.label"
        },
        {
          "value": "s",
          "label": "t:sections.all.section-margin.options__3.label"
        },
        {
          "value": "m",
          "label": "t:sections.all.section-margin.options__4.label"
        },
        {
          "value": "l",
          "label": "t:sections.all.section-margin.options__5.label"
        },
        {
          "value": "xl",
          "label": "t:sections.all.section-margin.options__6.label"
        }
      ],
      "default": "xs",
      "label": "t:sections.all.section-margin.margin_top.label"
    },
    {
      "type": "select",
      "id": "margin_bottom",
      "options": [
        {
          "value": "no-indent",
          "label": "t:sections.all.section-margin.options__1.label"
        },
        {
          "value": "xs",
          "label": "t:sections.all.section-margin.options__2.label"
        },
        {
          "value": "s",
          "label": "t:sections.all.section-margin.options__3.label"
        },
        {
          "value": "m",
          "label": "t:sections.all.section-margin.options__4.label"
        },
        {
          "value": "l",
          "label": "t:sections.all.section-margin.options__5.label"
        },
        {
          "value": "xl",
          "label": "t:sections.all.section-margin.options__6.label"
        }
      ],
      "default": "l",
      "label": "t:sections.all.section-margin.margin_bottom.label"
    }
  ],
  "blocks": [
    {
      "type": "@app"
    },
    {
      "type": "article-header",
      "name": "t:sections.main-article.blocks.article-header.name",
      "limit": 1,
      "settings": [
        {
          "type": "select",
          "id": "section_heading_tag",
          "label": "t:sections.all.heading.tag",
          "options": [
            {
              "value": "h1",
              "label": "H1"
            },
            {
              "value": "h2",
              "label": "H2"
            },
            {
              "value": "h3",
              "label": "H3"
            },
            {
              "value": "h4",
              "label": "H4"
            },
            {
              "value": "h5",
              "label": "H5"
            },
            {
              "value": "h6",
              "label": "H6"
            },
            {
              "value": "div",
              "label": "div"
            },
            {
              "value": "p",
              "label": "p"
            },
            {
              "value": "span",
              "label": "span"
            }
          ],
          "default": "h1"
        },
        {
          "type": "select",
          "id": "heading_size",
          "default": "h2",
          "label": "t:sections.all.heading_size.label",
          "options": [
            {
              "value": "h4",
              "label": "t:sections.all.heading_size.options__4.label"
            },
            {
              "value": "h3",
              "label": "t:sections.all.heading_size.options__1.label"
            },
            {
              "value": "h2",
              "label": "t:sections.all.heading_size.options__2.label"
            },
            {
              "value": "h1",
              "label": "t:sections.all.heading_size.options__3.label"
            }
          ]
        },
        {
          "type": "checkbox",
          "id": "blog_show_tags",
          "default": true,
          "label": "t:sections.main-article.blocks.article-header.settings.blog_show_tags.label"
        },
        {
          "type": "checkbox",
          "id": "blog_show_image",
          "default": true,
          "label": "t:sections.main-article.blocks.article-header.settings.blog_show_image.label"
        },
        {
          "type": "checkbox",
          "id": "blog_show_excerpt",
          "default": true,
          "label": "t:sections.main-article.blocks.article-header.settings.blog_show_excerpt.label"
        },
        {
          "type": "checkbox",
          "id": "blog_show_author",
          "default": true,
          "label": "t:sections.main-article.blocks.article-header.settings.blog_show_author.label"
        },
        {
          "type": "checkbox",
          "id": "blog_show_date",
          "default": true,
          "label": "t:sections.main-article.blocks.article-header.settings.blog_show_date.label"
        },
        {
          "type": "header",
          "content": "t:sections.main-article.blocks.article-header.settings.header__1.content"
        },
        {
          "type": "select",
          "id": "image_ratio",
          "default": "adapt",
          "label": "t:sections.all.image.aspect_ratio.label",
          "options": [
            {
              "value": "1/1",
              "label": "t:sections.all.image.aspect_ratio.options__1.label"
            },
            {
              "value": "3/2",
              "label": "t:sections.all.image.aspect_ratio.options__2.label"
            },
            {
              "value": "3/4",
              "label": "t:sections.all.image.aspect_ratio.options__3.label"
            },
            {
              "value": "4/3",
              "label": "t:sections.all.image.aspect_ratio.options__4.label"
            },
            {
              "value": "16/9",
              "label": "t:sections.all.image.aspect_ratio.options__5.label"
            },
            {
              "value": "21/9",
              "label": "t:sections.all.image.aspect_ratio.options__6.label"
            },
            {
              "value": "adapt",
              "label": "t:sections.all.image.aspect_ratio.options__7.label"
            }
          ]
        },
        {
          "type": "header",
          "content": "t:sections.main-article.blocks.article-header.settings.header__2.content"
        },
        {
          "type": "checkbox",
          "id": "show_share",
          "default": true,
          "label": "t:sections.main-article.settings.show_share.label"
        },
        {
          "type": "checkbox",
          "id": "show_share_label",
          "default": true,
          "label": "t:sections.main-article.settings.show_share_label.label"
        },
        {
          "type": "checkbox",
          "id": "show_share_facebook",
          "default": true,
          "label": "t:sections.main-article.settings.show_share_facebook.label"
        },
        {
          "type": "checkbox",
          "id": "show_share_twitter",
          "default": true,
          "label": "t:sections.main-article.settings.show_share_twitter.label"
        },
        {
          "type": "checkbox",
          "id": "show_share_telegram",
          "default": true,
          "label": "t:sections.main-article.settings.show_share_telegram.label"
        },
        {
          "type": "checkbox",
          "id": "show_share_reddit",
          "default": true,
          "label": "t:sections.main-article.settings.show_share_reddit.label"
        },
        {
          "type": "checkbox",
          "id": "show_share_copy",
          "default": true,
          "label": "t:sections.main-article.settings.show_share_copy.label"
        }
      ]
    },
    {
      "type": "content",
      "name": "t:sections.main-article.blocks.content.name",
      "limit": 1,
      "settings": []
    },
    {
      "type": "article-footer",
      "name": "t:sections.main-article.blocks.article-footer.name",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "blog_show_date",
          "default": true,
          "label": "t:sections.main-article.blocks.article-footer.settings.blog_show_date.label"
        },
        {
          "type": "header",
          "content": "t:sections.main-article.blocks.article-header.settings.header__2.content"
        },
        {
          "type": "checkbox",
          "id": "show_share",
          "default": true,
          "label": "t:sections.main-article.settings.show_share.label"
        },
        {
          "type": "checkbox",
          "id": "show_share_label",
          "default": true,
          "label": "t:sections.main-article.settings.show_share_label.label"
        },
        {
          "type": "checkbox",
          "id": "show_share_facebook",
          "default": true,
          "label": "t:sections.main-article.settings.show_share_facebook.label"
        },
        {
          "type": "checkbox",
          "id": "show_share_twitter",
          "default": true,
          "label": "t:sections.main-article.settings.show_share_twitter.label"
        },
        {
          "type": "checkbox",
          "id": "show_share_telegram",
          "default": true,
          "label": "t:sections.main-article.settings.show_share_telegram.label"
        },
        {
          "type": "checkbox",
          "id": "show_share_reddit",
          "default": true,
          "label": "t:sections.main-article.settings.show_share_reddit.label"
        },
        {
          "type": "checkbox",
          "id": "show_share_copy",
          "default": true,
          "label": "t:sections.main-article.settings.show_share_copy.label"
        }
      ]
    }
  ]
}
{% endschema %}
